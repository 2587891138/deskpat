# 桌面宠物 Claude AI 助手 — 实施计划

## Context

开发一个 2D 动漫人物桌面宠物应用，具备与 Claude AI 自然语言对话的能力。用户为开发者身份，目标是构建一个可用的跨平台桌面 AI 伴侣。

## 技术决策摘要

| 决策点 | 选择 |
|--------|------|
| 平台框架 | Electron + TypeScript |
| 脚手架 | electron-vite |
| UI 框架（对话窗口） | React + zustand |
| 宠物渲染 | Live2D Cubism SDK for Web (Canvas) |
| 角色模型 | Live2D 官方免费示例（Haru/Hiyori） |
| AI 集成 | 主进程代理调用 Anthropic Node SDK，IPC 通信 |
| 窗口布局 | 双窗口：宠物悬浮窗 + 独立对话窗口 |
| 交互方式 | 点击宠物弹出对话窗 + 全局快捷键 Ctrl+Shift+C |
| 数据存储 | SQLite（better-sqlite3） |
| 策略 | 分阶段：M1 能看能聊 → M2 能用 → M3 能交付 |

---

## M1 实现计划 — 能看能聊

### 架构概览

```
┌─────────────────────────────────────────────┐
│  主进程 (Main Process)                       │
│  ┌──────────┐  ┌──────────┐  ┌───────────┐ │
│  │ 窗口管理  │  │ IPC 桥接  │  │ SQLite DB  │ │
│  │- 宠物窗口 │  │- chat    │  │- 对话存储  │ │
│  │- 对话窗口 │  │- 事件    │  │- 会话管理  │ │
│  └──────────┘  └──────────┘  └───────────┘ │
│  ┌──────────────────────────────────────┐   │
│  │  Anthropic API 客户端                 │   │
│  │  - 流式响应处理                        │   │
│  │  - API Key 安全管理                    │   │
│  └──────────────────────────────────────┘   │
├─────────────────────────────────────────────┤
│  预加载脚本 (Preload)                        │
│  - contextBridge 暴露安全 API                │
├─────────────────────────────────────────────┤
│  渲染进程 (Renderer)                         │
│  ┌──────────────────┐  ┌────────────────┐  │
│  │  宠物窗口         │  │  对话窗口       │  │
│  │  - Live2D 渲染    │  │  - React UI    │  │
│  │  - 动画控制       │  │  - 消息列表     │  │
│  │  - 点击交互       │  │  - 输入框       │  │
│  └──────────────────┘  └────────────────┘  │
└─────────────────────────────────────────────┘
```

### 步骤 1：项目初始化

- 用 `npm create @quick-start/electron@latest` 初始化 electron-vite 项目
- 选择 TypeScript + React
- 安装依赖：`@anthropic-ai/sdk`、`better-sqlite3`、`zustand`
- 安装 Live2D Cubism SDK for Web（npm 包或手动引入）
- 配置 `electron-vite` 三进程（main/preload/renderer）

### 步骤 2：Live2D 模型资产管理

- 下载 Live2D 官方免费示例模型（Haru 或 Hiyori）
- 放置到 `assets/models/<name>/` 目录
- 在 electron-vite 中配置静态资源路径，确保模型文件可被渲染进程加载
- 编写模型加载器模块（初始化 Cubism SDK、加载 model3.json、绑定 Canvas）

### 步骤 3：宠物悬浮窗口

- 创建 frameless + transparent + alwaysOnTop 的 BrowserWindow
- 窗口尺寸约 400x500，初始位置屏幕右下角
- 实现 Canvas 渲染 Live2D 模型
- 实现基础动画：待机（呼吸）、被点击（反应动作）、思考中（特定动作）
- 实现窗口拖拽移动（mousedown/mousemove on Canvas）
- 右键菜单：隐藏宠物、打开对话窗口、退出

### 步骤 4：对话窗口

- 创建标准 BrowserWindow（可缩放、有标题栏）
- React 组件树：
  - `App` → `ChatWindow`
  - `ChatWindow` → `MessageList` + `InputArea`
  - `MessageList` → `MessageBubble[]`（用户/AI 消息样式区分）
  - `InputArea` → 输入框 + 发送按钮
- zustand store：`conversationStore`（当前会话、消息列表、加载状态）
- 流式消息渲染：AI 回复逐字显示

### 步骤 5：主进程 Claude API 集成

- 在主进程中初始化 `Anthropic` 客户端
- IPC handler：
  - `chat:send` — 发送消息，返回流式响应
  - `chat:stop` — 中断生成
- 实现 SSE 流式转发：`anthropic.messages.stream()` → `webContents.send('chat:chunk')`
- API Key 从环境变量或安全存储读取

### 步骤 6：预加载脚本安全桥接

- `contextBridge.exposeInMainWorld('electronAPI', { ... })`
- 暴露方法：
  - `sendMessage(content: string): Promise<void>`
  - `onChunk(callback): () => void`（返回取消订阅函数）
  - `stopGeneration(): void`
  - `openChatWindow(): void`
  - `getConversations(): Promise<Conversation[]>`
  - `getMessages(conversationId: string): Promise<Message[]>`

### 步骤 7：双窗口联动

- 点击宠物 → 主进程 `openChatWindow()`
- 对话窗口发消息 → 宠物做"思考"动画
- AI 回复完成 → 宠物做"回答"动画
- 宠物窗口关闭 → 对话窗口也关闭（可配置）
- 全局快捷键 `Ctrl+Shift+C` → 切换对话窗口显示/隐藏

### 步骤 8：SQLite 对话存储

- 在应用启动时初始化数据库文件（`userData/conversations.db`）
- 表结构：
  ```sql
  CREATE TABLE conversations (
    id TEXT PRIMARY KEY,
    title TEXT,
    created_at INTEGER,
    updated_at INTEGER
  );
  CREATE TABLE messages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    conversation_id TEXT REFERENCES conversations(id),
    role TEXT NOT NULL,  -- 'user' | 'assistant'
    content TEXT NOT NULL,
    created_at INTEGER
  );
  ```
- 实现数据访问层：`src/main/db.ts`
- 每次对话结束后自动保存

---

## 关键文件结构

```
desktop-pet-claude/
├── electron.vite.config.ts
├── package.json
├── tsconfig.json
├── assets/
│   └── models/
│       └── haru/                    # Live2D 模型文件
│           ├── haru.model3.json
│           ├── haru.1024/
│           ├── motions/
│           └── haru.physics3.json
├── src/
│   ├── main/                        # 主进程
│   │   ├── index.ts                 # 入口：创建窗口、注册 IPC
│   │   ├── windows/
│   │   │   ├── petWindow.ts         # 宠物悬浮窗口
│   │   │   └── chatWindow.ts        # 对话窗口
│   │   ├── ipc/
│   │   │   └── chat.ts              # 对话相关 IPC handlers
│   │   ├── ai/
│   │   │   └── anthropic.ts         # Anthropic API 客户端封装
│   │   └── db/
│   │       ├── connection.ts        # SQLite 连接管理
│   │       └── repository.ts        # 对话/消息 CRUD
│   ├── preload/
│   │   ├── index.ts                 # contextBridge 暴露
│   │   └── index.d.ts              # 类型声明
│   └── renderer/
│       ├── pet/                     # 宠物窗口（vanilla TS）
│       │   ├── index.html
│       │   ├── main.ts
│       │   └── live2d/
│       │       └── manager.ts       # Live2D 模型管理器
│       ├── chat/                    # 对话窗口（React）
│       │   ├── index.html
│       │   ├── main.tsx
│       │   ├── App.tsx
│       │   ├── components/
│       │   │   ├── ChatWindow.tsx
│       │   │   ├── MessageList.tsx
│       │   │   ├── MessageBubble.tsx
│       │   │   └── InputArea.tsx
│       │   └── store/
│       │       └── conversation.ts  # zustand store
│       └── shared/
│           └── api.ts               # window.electronAPI 类型封装
```

---

## 验证方法

1. **项目启动**：`npm run dev` 后宠物浮窗出现在桌面、可拖拽
2. **Live2D 渲染**：角色显示正常，待机动画播放，点击有反应
3. **对话框**：点击宠物或按 Ctrl+Shift+C 弹出对话窗口
4. **对话**：输入文字后 AI 流式回复，宠物在思考/回答状态切换
5. **持久化**：关闭重新打开，上次对话在列表中并可继续
6. **API Key**：未设置 Key 时提示用户配置，配置后正常工作